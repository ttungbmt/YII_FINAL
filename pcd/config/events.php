<?php
use common\events\AccessEvent;
use yii\base\Event;

//Event::on(\modules\pcd\pt_nguyco\controllers\DefaultController::className(), AccessEvent::EVENT_CHECK_GUEST, function ($event){
//    dd($event);
//});

//Event::on(\common\components\Access::className(), AccessEvent::EVENT_CHECK_GUEST, [AccessEvent::className(), 'checkGuest']);

//Event::on('*', Access::EVENT_CHECK_GUEST, function ($event) {
//    dd(111);
//});